# Runtime dependencies for the deployed Vercel function only.
# aerich is required even though its CLI is never invoked here: app/db.py's
# TORTOISE_ORM config imports aerich.models for schema tracking, so the
# package must be installed or the function crashes at startup.
fastapi
tortoise-orm
aerich
asyncpg
pydantic
pydantic-settings
python-dotenv
pyjwt
bcrypt
httpx
