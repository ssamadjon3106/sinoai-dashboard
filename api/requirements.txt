# Runtime dependencies for the deployed Vercel function only — mirrors
# server/requirements.txt minus dev-only tooling that has no business in the
# deployed bundle:
#   - uvicorn: Vercel's Python runtime invokes the ASGI app directly; there's
#     no process to bind a port on.
#   - aerich: a migration CLI, run locally/CI against the production DB, not
#     from the request-serving function itself.
# Keeping this list current with server/requirements.txt is a manual step —
# see server/requirements.txt for the source of truth on versions/comments.
fastapi
tortoise-orm
asyncpg
pydantic
pydantic-settings
python-dotenv
pyjwt
bcrypt
httpx
