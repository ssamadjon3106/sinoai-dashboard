# Base URL of the API in server/. Must match that server's PORT/CORS_ORIGIN.
# Leave this unset in Vercel's project settings — in production the frontend
# and the /api/* serverless function share one origin (see vercel.json), so
# src/api/http.ts defaults to a relative path automatically.
VITE_API_BASE_URL=http://localhost:8787
